#include <stdio.h>

int main(void){
    
    /* float c1, c2, c3, maior, equilatero, isosceles, escaleno;
    scanf("%f %f %f", &c1, &c2, &c3);

    maior = c1;
    if(c2 > maior){
        maior = c2;
    }
    if(c3 > maior){
        maior = c3;
    }
    equilatero = (c1 == c2 && c2 == c3);
    isosceles = (c1 == c2 || c1 == c3 || c2 == c3);
    escaleno = (c1 != c2 && c2 != c3);


    if(c1 > 0 && c2 > 0 && c3 > 0){
        if((c1 + c2) > maior || (c2 + c3) > maior || (c3 + c1) > maior){
            if(equilatero){
                printf("O triâgulo é equilátero.");
            }
                else {
                    if(escaleno){
                        printf("O triângulo é escaleno.");
                    }
                    if(isosceles){
                        printf("O triângulo é isósceles!");
                    }
                }
        }
    }


    int num;
    scanf("%d", &num);

    if(num%2 == 0){
        printf("O número é par!");
    }else {
        printf("O número é ímpar!");
    }


    int a, b, maior;
    scanf("%d %d", &a, &b);

    maior = a;
    if(b > maior){
        maior = b;
    }
    if(a == b){
        printf("a = b");
    }else {
        printf("O maior número é %d", maior);
    }


    int n1, n2;
    scanf("%d %d", &n1, &n2);
    if(n1 < n2){
        printf("%d %d", n1, n2);
    }
    else {
        printf("%d %d", n2, n1);
    } */
    return 0;
}